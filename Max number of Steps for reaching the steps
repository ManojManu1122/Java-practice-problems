Alice has to climb N stairs to reach top . In each step Alice can climb either 1 step or M
steps . Find the minimum number of steps to reach the top.

Code:

//pseudo code

public static int findClimbing(int m,int n){
  return(n/m+n%m);
}
